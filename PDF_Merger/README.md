# PDF Merger Project

Merge multiple PDF files into a single PDF using Python.

## Features
- Take user input for the number of PDF files to merge
- Merge PDFs in the order provided
- Save the combined PDF as a new file

## Technology
- Python
- `PyPDF2` module

## How to Run
1. Install Python and PyPDF2:
   ```bash
   pip install PyPDF2
